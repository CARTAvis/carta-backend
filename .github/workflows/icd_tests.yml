name: ICD tests
on:
  schedule:
    - cron: '0 0 * * *' # UTC time
env:
  ICD_RXJS_BRANCH_NAME: dev

jobs:

  Build:
    name: Build ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: source

      - name: System information (macOS)
        if: matrix.os == 'macos'
        shell: bash
        run: |
          uname -a
          sw_vers

      - name: Build carta-backend (macOS-14)
        if: matrix.os_version == 'macOS-14'
        shell: bash
        run: |
          export CC=/usr/local/Cellar/llvm/20.1.3/bin/clang
          export CXX=/usr/local/Cellar/llvm/20.1.3/bin/clang++
          export PROTOBUF_ROOT=$(brew --prefix protobuf@21)
          export PATH="$PROTOBUF_ROOT/bin:$PATH"
          export LIBRARY_PATH="$PROTOBUF_ROOT/lib:$LIBRARY_PATH"
          export CMAKE_PREFIX_PATH="$PROTOBUF_ROOT:$CMAKE_PREFIX_PATH"
          export PKG_CONFIG_PATH="$PROTOBUF_ROOT/lib/pkgconfig:$PKG_CONFIG_PATH"
          SRC_DIR=$GITHUB_WORKSPACE/source
          BUILD_DIR=$GITHUB_WORKSPACE/build
          cd $SRC_DIR && git submodule update --init
          rm -rf $BUILD_DIR && mkdir -p $BUILD_DIR
          cd $BUILD_DIR
          cmake $SRC_DIR \
            -Dtest=on \
            -DCMAKE_BUILD_TYPE=Debug \
            -DDevSuppressExternalWarnings=ON \
            -DCMAKE_CXX_FLAGS="-O0 -g -fsanitize=address -fno-omit-frame-pointer" \
            -DCMAKE_EXE_LINKER_FLAGS="-fsanitize=address"
          make -j 16

      - name: Build carta-backend (macOS)
        if: matrix.os == 'macos' && matrix.os_version != 'macOS-14'
        shell: bash
        run: |
          SRC_DIR=$GITHUB_WORKSPACE/source
          BUILD_DIR=$GITHUB_WORKSPACE/build
          cd $SRC_DIR && git submodule update --init
          rm -rf $BUILD_DIR && mkdir -p $BUILD_DIR
          cd $BUILD_DIR
          cmake $SRC_DIR \
            -Dtest=on \
            -DCMAKE_BUILD_TYPE=Debug \
            -DDevSuppressExternalWarnings=ON \
            -DCMAKE_CXX_FLAGS='-O0 -g -fsanitize=address -fno-omit-frame-pointer' \
            -DCMAKE_EXE_LINKER_FLAGS='-fsanitize=address'
          make -j 16

      - name: Build carta-backend (Linux)
        if: matrix.os == 'linux'
        shell: bash
        run: |
          SRC_DIR=$GITHUB_WORKSPACE/source
          BUILD_DIR=$GITHUB_WORKSPACE/build-${{ matrix.os_version }}
          rm -rf $BUILD_DIR && mkdir -p $BUILD_DIR
          cd source ; git submodule update --init
          apptainer exec --bind $GITHUB_WORKSPACE:$GITHUB_WORKSPACE --pwd $SRC_DIR ${{ matrix.image }} /bin/bash -c "\
            cd $BUILD_DIR && \
            cmake $SRC_DIR \
              -Dtest=on \
              -DCMAKE_BUILD_TYPE=Debug \
              -DDevSuppressExternalWarnings=ON \
              -DCMAKE_CXX_FLAGS='-O0 -g -fsanitize=address -fno-omit-frame-pointer' \
              -DCMAKE_EXE_LINKER_FLAGS='-fsanitize=address' && \
            make -j 16"

      - name: Check backend runs (macOS)
        if: matrix.os == 'macos'
        shell: bash
        run: |
          ./build/carta_backend --version

      - name: Check backend runs (Linux)
        if: matrix.os == 'linux'
        shell: bash
        run: |
          BUILD_DIR=$GITHUB_WORKSPACE/build-${{ matrix.os_version }}
          apptainer exec --bind $GITHUB_WORKSPACE:$GITHUB_WORKSPACE --pwd $BUILD_DIR ${{ matrix.image }} /bin/bash -c "./carta_backend --version"

  Prepare-ICD-RxJS:
    name: Prepare-ICD-RxJS ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: Build
    steps:
      - name: Checkout ICD-RxJS repository
        uses: actions/checkout@v4
        with:
          repository: CARTAvis/ICD-RxJS
          ref: ${{ env.ICD_RXJS_BRANCH_NAME }}
          path: ICD-RxJS

      - name: Prepare ICD-RxJS (macOS)
        if: matrix.os == 'macos'
        run: |
          ICD_DIR=$GITHUB_WORKSPACE/ICD-RxJS
          cd $ICD_DIR
          git submodule init && git submodule update && npm install
          cd protobuf
          ./build_proto.sh
          cd ../src/test
          perl -p -i -e 's/3002/5555/' config.json

      - name: Prepare ICD-RxJS (Linux)
        if: matrix.os == 'linux'
        run: |
          BUILD_DIR=$GITHUB_WORKSPACE/build-${{ matrix.os_version }}
          ICD_DIR=$GITHUB_WORKSPACE/ICD-RxJS
          cp -r $ICD_DIR $BUILD_DIR
          cd $BUILD_DIR/ICD-RxJS
          git submodule update --init
          apptainer exec --bind $GITHUB_WORKSPACE:$GITHUB_WORKSPACE --pwd $BUILD_DIR ${{ matrix.image }} /bin/bash -c "\
            cd ICD-RxJS && \
            npm install && \
            cd protobuf && \
            ./build_proto.sh && \
            cd ../src/test && \
            perl -p -i -e 's/3002/${{ matrix.port }}/' config.json"

  File-Browser-ICD-Tests:
    name: File-Browser ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: Prepare-ICD-RxJS
    if: always()
    steps:
      # macOS steps
      - name: File Browser ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'file_browser'
      # Linux steps    
      - name: File Browser ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'file_browser'

  Animator-ICD-Tests:
    name: Animator ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [File-Browser-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Animator ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'animator'
      # Linux steps
      - name: Animator ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'animator'

  Region-Statistics-ICD-Tests:
    name: Region Statistics ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Animator-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Region-Statistics ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'region_statistics'
      # Linux steps
      - name: Region-Statistics ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'region_statistics'

  Region-Manipulation-ICD-Tests:
    name: Region Manipulation ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Region-Statistics-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Region Manipulation ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'region_manipulation'
      # Linux steps
      - name: Region Manipulation ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'region_manipulation'

  Cube-Histogram-ICD-Tests:
    name: Cube Histogram ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Region-Manipulation-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Cube Histogram ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'cube_histogram'
      # Linux steps
      - name: Cube Histogram ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'cube_histogram'

  PV-Generator-ICD-Tests:
    name: PV Generator ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Cube-Histogram-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: PV Generator ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'pv_generator'
      # Linux steps
      - name: PV Generator ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'pv_generator'

  Raster-Tiles-ICD-Tests:
    name: Raster Tiles ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:  
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [PV-Generator-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Raster Tiles ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'raster_tiles'
      # Linux steps
      - name: Raster Tiles ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'raster_tiles'

  Catalog-ICD-Tests:
    name: Catalog ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Raster-Tiles-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Catalog ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'catalog'
      # Linux steps
      - name: Catalog ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'catalog'

  Moment-ICD-Tests:
    name: Moment ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Catalog-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Moment ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'moment'
      # Linux steps
      - name: Moment ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'moment'

  Match-ICD-Tests:
    name: Match ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9     
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Moment-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Match ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'match'
      # Linux steps
      - name: Match ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'match'

  Close-File-ICD-Tests:
    name: Close File ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:  
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9   
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Match-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Close File ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'close_file'
      # Linux steps
      - name: Close File ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'close_file'

  Image-Fitting-ICD-Tests:
    name: Image Fitting ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        include:  
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9   
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Close-File-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Image Fitting ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'image_fitting'
      # Linux steps
      - name: Image Fitting ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'image_fitting'

  Vector-Overlay-ICD-Tests:
    name: Vector Overlay ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:  
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9   
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Image-Fitting-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Vector Overlay ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'vector_overlay'
      # Linux steps
      - name: Vector Overlay ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'vector_overlay'

  Resume-ICD-Tests:
    name: Resume ${{ matrix.os_version }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        include:  
          - os_version: macOS-13
            os: macos
            runner: [macOS-13, ICD]
          - os_version: macOS-14
            os: macos
            runner: [macOS-14, ICD]
          - os_version: macOS-15
            os: macos
            runner: [macOS-15, ICD]
          - os_version: ubuntu-22.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD2]
            image: /opt/apptainer/ubuntu-2204-dec2023.sif
            port: 9002
          - os_version: ubuntu-24.04
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD6]
            image: /opt/apptainer/ubuntu-2404-apr2025.sif
            port: 9006
          - os_version: rhel-8
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD4]
            image: /opt/apptainer/almalinux8-dec2023.sif
            port: 9004
          - os_version: rhel-9
            os: linux
            runner: [self-hosted, Linux, Apptainer, ICD5]
            image: /opt/apptainer/almalinux9-dec2023.sif
            port: 9005
    needs: [Vector-Overlay-ICD-Tests, Prepare-ICD-RxJS]
    if: always()
    steps:
      # macOS steps
      - name: Resume ICD tests
        if: matrix.os == 'macos'
        uses: ./source/.github/actions/run-macos
        with:
          test_stage_name: 'resume'
      # Linux steps
      - name: Resume ICD tests
        if: matrix.os == 'linux'
        uses: ./source/.github/actions/run-apptainer
        with:
          os_version: ${{ matrix.os_version }}
          image: ${{ matrix.image }}
          port: ${{ matrix.port }}
          test_stage_name: 'resume'

  NotifyEmail:
    name: Send email on failure
    runs-on: ubuntu-latest
    needs:
      - Build macOS-13
      - Build macOS-14
      - Build macOS-15
      - Build ubuntu-22.04
      - Build ubuntu-24.04
      - Build rhel-8
      - Build rhel-9
      - Prepare-ICD-RxJS macOS-13
      - Prepare-ICD-RxJS macOS-14
      - Prepare-ICD-RxJS macOS-15
      - Prepare-ICD-RxJS ubuntu-22.04
      - Prepare-ICD-RxJS ubuntu-24.04
      - Prepare-ICD-RxJS rhel-8
      - Prepare-ICD-RxJS rhel-9
      - File-Browser macOS-13
      - File-Browser macOS-14
      - File-Browser macOS-15
      - File-Browser ubuntu-22.04
      - File-Browser ubuntu-24.04
      - File-Browser rhel-8
      - File-Browser rhel-9
      - Animator macOS-13
      - Animator macOS-14
      - Animator macOS-15
      - Animator ubuntu-22.04
      - Animator ubuntu-24.04
      - Animator rhel-8
      - Animator rhel-9
      - Region Statistics macOS-13
      - Region Statistics macOS-14
      - Region Statistics macOS-15
      - Region Statistics ubuntu-22.04
      - Region Statistics ubuntu-24.04
      - Region Statistics rhel-8
      - Region Statistics rhel-9
      - Region Manipulation macOS-13
      - Region Manipulation macOS-14
      - Region Manipulation macOS-15
      - Region Manipulation ubuntu-22.04
      - Region Manipulation ubuntu-24.04
      - Region Manipulation rhel-8
      - Region Manipulation rhel-9
      - Cube Histogram macOS-13
      - Cube Histogram macOS-14
      - Cube Histogram macOS-15
      - Cube Histogram ubuntu-22.04
      - Cube Histogram ubuntu-24.04
      - Cube Histogram rhel-8
      - Cube Histogram rhel-9
      - PV Generator macOS-13
      - PV Generator macOS-14
      - PV Generator macOS-15
      - PV Generator ubuntu-22.04
      - PV Generator ubuntu-24.04
      - PV Generator rhel-8
      - PV Generator rhel-9
      - Raster Tiles macOS-13
      - Raster Tiles macOS-14
      - Raster Tiles macOS-15
      - Raster Tiles ubuntu-22.04
      - Raster Tiles ubuntu-24.04
      - Raster Tiles rhel-8
      - Raster Tiles rhel-9
      - Catalog macOS-13
      - Catalog macOS-14
      - Catalog macOS-15
      - Catalog ubuntu-22.04
      - Catalog ubuntu-24.04
      - Catalog rhel-8
      - Catalog rhel-9
      - Moment macOS-13
      - Moment macOS-14
      - Moment macOS-15
      - Moment ubuntu-22.04
      - Moment ubuntu-24.04
      - Moment rhel-8
      - Moment rhel-9
      - Match macOS-13
      - Match macOS-14
      - Match macOS-15
      - Match ubuntu-22.04
      - Match ubuntu-24.04
      - Match rhel-8
      - Match rhel-9
      - Close File macOS-13
      - Close File macOS-14
      - Close File macOS-15
      - Close File ubuntu-22.04
      - Close File ubuntu-24.04
      - Close File rhel-8
      - Close File rhel-9
      - Image Fitting macOS-13
      - Image Fitting macOS-14
      - Image Fitting macOS-15
      - Image Fitting ubuntu-22.04
      - Image Fitting ubuntu-24.04
      - Image Fitting rhel-8
      - Image Fitting rhel-9
      - Vector Overlay macOS-13
      - Vector Overlay macOS-14
      - Vector Overlay macOS-15
      - Vector Overlay ubuntu-22.04
      - Vector Overlay ubuntu-24.04
      - Vector Overlay rhel-8
      - Vector Overlay rhel-9
      - Resume macOS-13
      - Resume macOS-14
      - Resume macOS-15
      - Resume ubuntu-22.04
      - Resume ubuntu-24.04
      - Resume rhel-8
      - Resume rhel-9
    if: failure()
    steps:
      - name: Send email notification
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "CARTA CI Failed: ${{ github.repository }} @ ${{ github.ref }}"
          to: ${{ secrets.MAIL_TO }} ${{ secrets.MAIL_USERNAME }}
          from: "CARTA CI <${{ secrets.MAIL_USERNAME }}>"
          body: |
            Workflow failed

            Repository: ${{ github.repository }}
            Branch/Ref: ${{ github.ref }}
            Workflow: ${{ github.workflow }}
            Run ID: ${{ github.run_id }}
            URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            Failed jobs:
            - Build macOS-13: ${{ needs['Build macOS-13'].result }}
            - Build macOS-14: ${{ needs['Build macOS-14'].result }}
            - Build macOS-15: ${{ needs['Build macOS-15'].result }}
            - Build ubuntu-22.04: ${{ needs['Build ubuntu-22.04'].result }}
            - Build ubuntu-24.04: ${{ needs['Build ubuntu-24.04'].result }}
            - Build rhel-8: ${{ needs['Build rhel-8'].result }}
            - Build rhel-9: ${{ needs['Build rhel-9'].result }}
            - Prepare-ICD-RxJS macOS-13: ${{ needs['Prepare-ICD-RxJS macOS-13'].result }}
            - Prepare-ICD-RxJS macOS-14: ${{ needs['Prepare-ICD-RxJS macOS-14'].result }}
            - Prepare-ICD-RxJS macOS-15: ${{ needs['Prepare-ICD-RxJS macOS-15'].result }}
            - Prepare-ICD-RxJS ubuntu-22.04: ${{ needs['Prepare-ICD-RxJS ubuntu-22.04'].result }}
            - Prepare-ICD-RxJS ubuntu-24.04: ${{ needs['Prepare-ICD-RxJS ubuntu-24.04'].result }}
            - Prepare-ICD-RxJS rhel-8: ${{ needs['Prepare-ICD-RxJS rhel-8'].result }}
            - Prepare-ICD-RxJS rhel-9: ${{ needs['Prepare-ICD-RxJS rhel-9'].result }}
            - File-Browser macOS-13: ${{ needs['File-Browser macOS-13'].result }}
            - File-Browser macOS-14: ${{ needs['File-Browser macOS-14'].result }}
            - File-Browser macOS-15: ${{ needs['File-Browser macOS-15'].result }}
            - File-Browser ubuntu-22.04: ${{ needs['File-Browser ubuntu-22.04'].result }}
            - File-Browser ubuntu-24.04: ${{ needs['File-Browser ubuntu-24.04'].result }}
            - File-Browser rhel-8: ${{ needs['File-Browser rhel-8'].result }}
            - File-Browser rhel-9: ${{ needs['File-Browser rhel-9'].result }}
            - Animator macOS-13: ${{ needs['Animator macOS-13'].result }}
            - Animator macOS-14: ${{ needs['Animator macOS-14'].result }}
            - Animator macOS-15: ${{ needs['Animator macOS-15'].result }}
            - Animator ubuntu-22.04: ${{ needs['Animator ubuntu-22.04'].result }}
            - Animator ubuntu-24.04: ${{ needs['Animator ubuntu-24.04'].result }}
            - Animator rhel-8: ${{ needs['Animator rhel-8'].result }}
            - Animator rhel-9: ${{ needs['Animator rhel-9'].result }}
            - Region Statistics macOS-13: ${{ needs['Region Statistics macOS-13'].result }}
            - Region Statistics macOS-14: ${{ needs['Region Statistics macOS-14'].result }}
            - Region Statistics macOS-15: ${{ needs['Region Statistics macOS-15'].result }}
            - Region Statistics ubuntu-22.04: ${{ needs['Region Statistics ubuntu-22.04'].result }}
            - Region Statistics ubuntu-24.04: ${{ needs['Region Statistics ubuntu-24.04'].result }}
            - Region Statistics rhel-8: ${{ needs['Region Statistics rhel-8'].result }}
            - Region Statistics rhel-9: ${{ needs['Region Statistics rhel-9'].result }}
            - Region Manipulation macOS-13: ${{ needs['Region Manipulation macOS-13'].result }}
            - Region Manipulation macOS-14: ${{ needs['Region Manipulation macOS-14'].result }}
            - Region Manipulation macOS-15: ${{ needs['Region Manipulation macOS-15'].result }}
            - Region Manipulation ubuntu-22.04: ${{ needs['Region Manipulation ubuntu-22.04'].result }}
            - Region Manipulation ubuntu-24.04: ${{ needs['Region Manipulation ubuntu-24.04'].result }}
            - Region Manipulation rhel-8: ${{ needs['Region Manipulation rhel-8'].result }}
            - Region Manipulation rhel-9: ${{ needs['Region Manipulation rhel-9'].result }}
            - Cube Histogram macOS-13: ${{ needs['Cube Histogram macOS-13'].result }}
            - Cube Histogram macOS-14: ${{ needs['Cube Histogram macOS-14'].result }}
            - Cube Histogram macOS-15: ${{ needs['Cube Histogram macOS-15'].result }}
            - Cube Histogram ubuntu-22.04: ${{ needs['Cube Histogram ubuntu-22.04'].result }}
            - Cube Histogram ubuntu-24.04: ${{ needs['Cube Histogram ubuntu-24.04'].result }}
            - Cube Histogram rhel-8: ${{ needs['Cube Histogram rhel-8'].result }}
            - Cube Histogram rhel-9: ${{ needs['Cube Histogram rhel-9'].result }}
            - PV Generator macOS-13: ${{ needs['PV Generator macOS-13'].result }}
            - PV Generator macOS-14: ${{ needs['PV Generator macOS-14'].result }}
            - PV Generator macOS-15: ${{ needs['PV Generator macOS-15'].result }}
            - PV Generator ubuntu-22.04: ${{ needs['PV Generator ubuntu-22.04'].result }}
            - PV Generator ubuntu-24.04: ${{ needs['PV Generator ubuntu-24.04'].result }}
            - PV Generator rhel-8: ${{ needs['PV Generator rhel-8'].result }}
            - PV Generator rhel-9: ${{ needs['PV Generator rhel-9'].result }}
            - Raster Tiles macOS-13: ${{ needs['Raster Tiles macOS-13'].result }}
            - Raster Tiles macOS-14: ${{ needs['Raster Tiles macOS-14'].result }}
            - Raster Tiles macOS-15: ${{ needs['Raster Tiles macOS-15'].result }}
            - Raster Tiles ubuntu-22.04: ${{ needs['Raster Tiles ubuntu-22.04'].result }}
            - Raster Tiles ubuntu-24.04: ${{ needs['Raster Tiles ubuntu-24.04'].result }}
            - Raster Tiles rhel-8: ${{ needs['Raster Tiles rhel-8'].result }}
            - Raster Tiles rhel-9: ${{ needs['Raster Tiles rhel-9'].result }}
            - Catalog macOS-13: ${{ needs['Catalog macOS-13'].result }}
            - Catalog macOS-14: ${{ needs['Catalog macOS-14'].result }}
            - Catalog macOS-15: ${{ needs['Catalog macOS-15'].result }}
            - Catalog ubuntu-22.04: ${{ needs['Catalog ubuntu-22.04'].result }}
            - Catalog ubuntu-24.04: ${{ needs['Catalog ubuntu-24.04'].result }}
            - Catalog rhel-8: ${{ needs['Catalog rhel-8'].result }}
            - Catalog rhel-9: ${{ needs['Catalog rhel-9'].result }}
            - Moment macOS-13: ${{ needs['Moment macOS-13'].result }}
            - Moment macOS-14: ${{ needs['Moment macOS-14'].result }}
            - Moment macOS-15: ${{ needs['Moment macOS-15'].result }}
            - Moment ubuntu-22.04: ${{ needs['Moment ubuntu-22.04'].result }}
            - Moment ubuntu-24.04: ${{ needs['Moment ubuntu-24.04'].result }}
            - Moment rhel-8: ${{ needs['Moment rhel-8'].result }}
            - Moment rhel-9: ${{ needs['Moment rhel-9'].result }}
            - Match macOS-13: ${{ needs['Match macOS-13'].result }}
            - Match macOS-14: ${{ needs['Match macOS-14'].result }}
            - Match macOS-15: ${{ needs['Match macOS-15'].result }}
            - Match ubuntu-22.04: ${{ needs['Match ubuntu-22.04'].result }}
            - Match ubuntu-24.04: ${{ needs['Match ubuntu-24.04'].result }}
            - Match rhel-8: ${{ needs['Match rhel-8'].result }}
            - Match rhel-9: ${{ needs['Match rhel-9'].result }}
            - Close File macOS-13: ${{ needs['Close File macOS-13'].result }}
            - Close File macOS-14: ${{ needs['Close File macOS-14'].result }}
            - Close File macOS-15: ${{ needs['Close File macOS-15'].result }}
            - Close File ubuntu-22.04: ${{ needs['Close File ubuntu-22.04'].result }}
            - Close File ubuntu-24.04: ${{ needs['Close File ubuntu-24.04'].result }}
            - Close File rhel-8: ${{ needs['Close File rhel-8'].result }}
            - Close File rhel-9: ${{ needs['Close File rhel-9'].result }}
            - Image Fitting macOS-13: ${{ needs['Image Fitting macOS-13'].result }}
            - Image Fitting macOS-14: ${{ needs['Image Fitting macOS-14'].result }}
            - Image Fitting macOS-15: ${{ needs['Image Fitting macOS-15'].result }}
            - Image Fitting ubuntu-22.04: ${{ needs['Image Fitting ubuntu-22.04'].result }}
            - Image Fitting ubuntu-24.04: ${{ needs['Image Fitting ubuntu-24.04'].result }}
            - Image Fitting rhel-8: ${{ needs['Image Fitting rhel-8'].result }}
            - Image Fitting rhel-9: ${{ needs['Image Fitting rhel-9'].result }}
            - Vector Overlay macOS-13: ${{ needs['Vector Overlay macOS-13'].result }}
            - Vector Overlay macOS-14: ${{ needs['Vector Overlay macOS-14'].result }}
            - Vector Overlay macOS-15: ${{ needs['Vector Overlay macOS-15'].result }}
            - Vector Overlay ubuntu-22.04: ${{ needs['Vector Overlay ubuntu-22.04'].result }}
            - Vector Overlay ubuntu-24.04: ${{ needs['Vector Overlay ubuntu-24.04'].result }}
            - Vector Overlay rhel-8: ${{ needs['Vector Overlay rhel-8'].result }}
            - Vector Overlay rhel-9: ${{ needs['Vector Overlay rhel-9'].result }}
            - Resume macOS-13: ${{ needs['Resume macOS-13'].result }}
            - Resume macOS-14: ${{ needs['Resume macOS-14'].result }}
            - Resume macOS-15: ${{ needs['Resume macOS-15'].result }}
            - Resume ubuntu-22.04: ${{ needs['Resume ubuntu-22.04'].result }}
            - Resume ubuntu-24.04: ${{ needs['Resume ubuntu-24.04'].result }}
            - Resume rhel-8: ${{ needs['Resume rhel-8'].result }}
            - Resume rhel-9: ${{ needs['Resume rhel-9'].result }}
